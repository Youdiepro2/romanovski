{% include 'default/_form.html.twig' with {
    page_title: 'Edit your task',
    page_title_param: {'%id%': task.id},
    attr: {
        method: 'PUT',
        action: url('task_edit', {'id': task.id}),
    },
    form: form,
    back_to_list_path: 'task_index',
} %}