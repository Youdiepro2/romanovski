{% include 'default/_form.html.twig' with {
    page_title: 'Delete this task?',
    page_title_param: {'%id%': task.id},
    attr: {
        method: 'DELETE',
        action: url('task_delete', {'id': task.id}),
    },
    form: form,
    submit_label: 'Delete'|trans,
    back_to_list_path: 'task_index',
} %}