{% extends 'base.html.twig' %}

{% block title %}
    {{ 'Category_details'|trans({'%id%': category.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'Category details:'|trans({'%id%': category.id|default('')}) }}</h1>
    {% if category is defined and category|length %}
        <dl class="dl-horizontal">
            <dt>{{ 'id'|trans }}</dt>
            <dd>{{ category.id }}</dd>
            <dt>{{ 'Created_at'|trans }}</dt>
            <dd>{{ category.createdAt|date('Y/m/d') }}</dd>
            <dt>{{ 'Updated_at'|trans }}</dt>
            <dd>{{ category.updatedAt|date('Y/m/d') }}</dd>
            <dt>{{ 'Title:'|trans }}</dt>
            <dd>{{  category.title }}</dd>
            <dt>{{ 'label_tasks'|trans }}</dt>
            <dd>
                {% for task in category.tasks %}
                    <a href="{{ url('task_show', {id: task.id}) }}" title="{{ 'task_show'|trans }}">
                        {{ task.title }}
                    </a>|
                {%  endfor %}
            </dd>
        </dl>
        <p>
            <a class="btn btn-primary" href="{{ url('category_index') }}" title="{{ 'action_back_to_list'|trans }}">
                {{ 'Back to list'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message_item_not_found'|trans }}
        </p>
    {% endif %}
{% endblock %}